# AGENTS.md

## 1. このファイルについて

このファイルは、このプロジェクトでAIエージェントが作業する際の基本ルールです。

プロジェクト固有の仕様については、以下の資料を参照してください。

* `src` フォルダを新規作成し、ルートにある `file_list.py` を `src` へ移動してください。 移動が完了したら `test` フォルダを作成し、最後にコミットとリモートへのプッシュまで行ってください。

資料が存在しない場合は、必要に応じてユーザーに確認してください。

---

# 2. 基本方針

* 作業開始前に、関連する設計書・仕様書を確認する。
* 既存コードを十分に確認してから変更する。
* 設計書と既存コードに矛盾がある場合、勝手に仕様を変更しない。
* 不明点や重大な設計上の問題がある場合は、実装を開始する前にユーザーへ確認する。
* 小さな実装上の判断は適切に行ってよいが、重要な仕様・設計判断はユーザーの承認を得る。
* 既存機能を意図せず変更・破壊しない。
* 作業後は可能な範囲でテストを実行する。
* テスト結果を確認せずに「完了」と判断しない。

---

# 3. 作業開始時

作業開始時には、可能な範囲で以下を確認する。

1. `AGENTS.md`
2. `docs/design.md`
3. `docs/requirements.md`
4. `docs/decisions.md`
5. `docs/progress.md`
6. 現在のGitの状態
7. 関連するソースコード・テストコード

特に `docs/progress.md` に未完了の作業がある場合は、そこから作業を再開する。

前回の作業状態が不明な場合、推測で大量の変更を行わず、まず現在の状態を確認する。

---

# 4. 設計変更のルール

設計書に記載された仕様を基本的に維持する。

以下の場合は、勝手に仕様変更を確定しない。

* データ構造を変更する
* 外部インターフェースを変更する
* API仕様を変更する
* ユーザー操作を変更する
* 既存機能の挙動を変更する
* セキュリティ上の重要な判断を行う
* 将来の拡張性に大きく影響する変更を行う

このような問題を発見した場合は、

1. 問題点
2. 現在の設計で発生する問題
3. 推奨する変更案
4. 代替案
5. 変更による影響

を簡潔に説明し、ユーザーの判断を求める。

---

# 5. 設計判断の記録

重要な設計判断を行った場合は `docs/decisions.md` に記録する。

記録対象の例：

* 技術・ライブラリの選択
* アーキテクチャの選択
* データ構造の変更
* API方式の選択
* 重要な実装方式の選択
* 設計書から変更した理由
* 採用しなかった代替案
* バグ修正によって仕様・設計が変更された場合

単なる一時的な試行錯誤や、変数名の変更など、将来の判断に影響しない内容は記録しない。

記録形式：

```markdown
## YYYY-MM-DD

### 判断
何を決定したか。

### 理由
なぜこの方法を採用したか。

### 検討した代替案
- A案
- B案

### 影響
この判断によって何が変わるか。
```

重要な判断については、可能な限り「なぜそうしたのか」を残す。

---

# 6. 作業進捗の記録

`docs/progress.md` を、作業状態を引き継ぐための記録として使用する。

長時間の作業では、適切な区切りごとに更新する。

最低限、以下を記録する。

```markdown
# 作業進捗

## 現在の状態

現在どこまで完了しているか。

## 完了した作業

- ...

## 作業中

- ...

## 未完了

- ...

## テスト状況

- 実行したテスト
- 成功／失敗
- 未実施のテスト

## 次に行う作業

1. ...
2. ...
3. ...

## 注意事項

次回作業時に確認すべき事項。
```

---

# 7. 作業中断への対応

エージェントの利用枠、セッション、環境、時間などの理由で作業が途中で終了する可能性を考慮する。

作業途中でも、重要な区切りに到達した場合は `docs/progress.md` を更新する。

特に以下の場合は進捗を記録する。

* 大きな機能の実装が完了した
* テストを実行した
* テストが失敗した
* 設計上の問題を発見した
* 設計判断を行った
* 次の作業段階へ移った
* 長時間の作業を終了する前

作業が中断された場合でも、次回のエージェントが `docs/progress.md` を読むことで、可能な限りスムーズに作業を再開できる状態を維持する。

作業途中の状態を「完了」と記録してはいけない。

---

# 8. 作業再開時

前回の作業が中断されていた場合：

1. `docs/progress.md` を読む
2. Gitの状態を確認する
3. 実際のソースコードと進捗記録を照合する
4. 未完了の作業を特定する
5. 必要なテストを再実行する
6. 状態を確認してから続きを実施する

`progress.md` の記録と実際のコードの状態が一致しない場合は、コードの状態を優先して調査する。

前回の作業を最初からやり直すのではなく、既に完了している作業を確認したうえで続きを行う。

---

# 9. テスト

可能な限り、変更後に関連するテストを実行する。

テストが失敗した場合：

* エラー内容を確認する
* 原因を調査する
* 修正する
* 再度テストする

テストを実行できない場合は、その理由を `docs/progress.md` に記録する。

テストを実行していない状態で、テスト済みであるかのように報告してはいけない。

---

# 10. コード変更

変更は必要最小限にする。

無関係なコードのリファクタリングやフォーマット変更を、作業のついでに大量に行わない。

既存コードを変更する場合は、変更前の挙動を可能な範囲で確認する。

大規模な変更が必要になった場合は、作業を段階に分ける。

---

# 11. 完了条件

作業を「完了」と報告する前に、可能な範囲で以下を確認する。

* [ ] 設計書を確認した
* [ ] 実装が完了した
* [ ] 関連テストを実行した
* [ ] テスト結果を確認した
* [ ] 必要な設計判断を `decisions.md` に記録した
* [ ] `progress.md` を更新した
* [ ] 未完了事項があれば明記した

すべてを実施できない場合は、何が未実施なのかを明確に報告する。

---

# 12. ユーザーへの報告

作業終了時には、簡潔に以下を報告する。

### 実施したこと

* ...

### テスト

* ...

### 設計判断

* ...

### 未完了・問題

* ...

### 次に行うこと

* ...

特に作業途中で終了する場合は、

**「どこまで完了し、次に何をすればよいか」**

を明確にする。

git add .及びcommit、pushまでAIエージェントで実施するが、どの操作も必ずユーザーの許可を得てから実施すること。
