# 設計メモ・変更履歴

- 2026-08-29: `src/file_list.py` を追加し、指定ディレクトリ内のファイル名を一覧表示する簡易スクリプトを実装した。引数が未指定の場合はカレントディレクトリを対象にし、存在しないパスやファイルパスに対するエラー処理も含めた。`AGENTS.md` の指示に従い、変更理由と変更内容を記録する。
- 2026-08-29: `src/` と `test/` を新設し、ファイル配置を整理した。ソースコードとテストを分離し、今後の保守性と検証性を高めるため。
