# 要件定義

## 目的

指定ディレクトリ内のファイル名を簡単に一覧表示できる Python スクリプトを提供する。

## 機能要件

- 引数でディレクトリを受け取る
- 引数が省略された場合はカレントディレクトリを対象とする
- ファイルのみを表示し、サブディレクトリは対象外とする
- 表示順はファイル名順にソートする
- 存在しないパスやファイルパスを指定した場合はエラーを出力して終了コード 1 を返す

## 非機能要件

- 依存ライブラリを使わずに標準ライブラリのみで実装する
- リポジトリの構造を明確に保つため、 source と test を分離する
